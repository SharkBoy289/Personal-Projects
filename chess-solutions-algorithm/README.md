# Chess Solutions Algorithm

Python playground for experimenting with chess problem solvers.

## Development
Install dependencies (if any) and run `python main.py`.

## Deployment
Package the Python code or expose it through an API as needed.
