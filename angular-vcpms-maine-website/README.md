# VCPMS Maine Website

Angular scaffold for the VCPMS Maine website.

## Development
Install dependencies with `npm install` and start the dev server with `npm start`.

## Deployment
Run `npm run build` to produce static files for deployment.
