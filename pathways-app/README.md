# Pathways App

Next.js scaffold for exploring education and career pathways.

## Development
Install dependencies with `npm install` and start the dev server with `npm run dev`.

## Deployment
Build with `npm run build` and deploy the `.next` output using a Node.js server or a platform like Vercel.
