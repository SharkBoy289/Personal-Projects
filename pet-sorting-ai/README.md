# Pet Sorting AI

Experiment with models that classify or sort pets based on traits.

## Development
Run `python main.py` to execute the sample code.

## Deployment
Package the model behind an API or integrate into an application.
