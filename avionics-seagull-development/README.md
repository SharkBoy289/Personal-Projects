# Avionics Seagull Development

C++ experiments for avionics-related code.

## Development
Compile with `g++ src/main.cpp -o seagull` and run `./seagull`.

## Deployment
Flash the compiled binary to the target hardware or integrate into flight software.
